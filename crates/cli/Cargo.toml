[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.40"
clap = { version = "4.0", features = ["derive"] }
log = "0.4"
schema_core = { path = "../schema-core" }
env_logger = "0.10"
tokio = { version = "1", features = ["full"] }
